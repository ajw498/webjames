 - Give a more visible error if unable to create socket to listen on
 - Directory indexes
 - Icon bar icon?
 - GUI for choices or a web configuration frontend
 - Act as a proxy / cache
 - Better support for encodings
 - HTTP 1.1
 - HTTPS
 - Resolve allow-hosts list?

 - If report() is called then some temporary buffers/cgi redirection
   files do not get freed/deleted
